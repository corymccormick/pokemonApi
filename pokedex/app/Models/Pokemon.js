export default class Pokemon {
  constructor({ name, img, weight, height, types }) {
    this.name = name || species.name.join
    this.img = img || Sprites.other.official - artwork.front_default.join
    this.weight = weight
    this.height = height
    this.types = types

  }

  get Template() {

  }
}

